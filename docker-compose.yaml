version: '3'

services:

  mysql:
    image: mysql:8.0.30-debian
    platform: linux/x86_64
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=events